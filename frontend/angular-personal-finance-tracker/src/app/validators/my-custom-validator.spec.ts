import { MyCustomValidator } from './my-custom-validator';

describe('MyCustomValidator', () => {
  it('should create an instance', () => {
    expect(new MyCustomValidator()).toBeTruthy();
  });
});
