import { BudgetWithSpending } from './budget-with-spending';

describe('BudgetWithSpending', () => {
  it('should create an instance', () => {
    expect(new BudgetWithSpending()).toBeTruthy();
  });
});
